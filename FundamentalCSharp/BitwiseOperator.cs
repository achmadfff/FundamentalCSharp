/*
 * Bitwise OPERATOR
 * Operator yang bermain di bit level operation pada integer. Operator ini digunakan untuk memanipulasi individual bits
 * yang akan mengubah nilai integer
 * 
 * Contoh 
 * 0 dan 1
 * 1. & (bitwise AND)   : operator ini melakukan komparasi setiap bit pada operand pertama.
 * 2. | (bitwise OR)    : operator ini melakukan komparasi setiap bit pada operand pertama.
 * 3. ^ (bitwise XOR)   : operator ini melakukan komparasi setiap bit pada operand pertama. Jika bit berbeda result bit akan menjadi
 *                        1 dan kebalikannya adalah 0
 * 4. ~ (bitwise NOT)   : Operator ini akan invert semua bit dalam operand
 * 5. << (Left Shift)   : Operator ini akan mengganti bits pada operand pertama kiri sesuai angka yang diberikan
 * 6. >> (Right Shift)  : Operator ini akan mengganti bits pada operand pertama kanan sesuai angka yang diberikan
 */
//int x = 1, y = 4, result;

//result = x & y;
//Console.WriteLine("Bitwise AND : " + result);
//result = x | y;
//Console.WriteLine("Bitwise OR : " + result);
//result = x ^ y;
//Console.WriteLine("Bitwise XOR : " + result);
//result = ~x;
//Console.WriteLine("Bitwise NOT : " + result);
//result = x << 2;
//Console.WriteLine("Bitwise Left Shift : " + result);
//result = x >> 2;
//Console.WriteLine("Bitwise Right Shift : " + result);